function userInList(){
 var unList = [];
 var adduser = (document.getElementById('jellybean').value);
 unList.push(adduser);
for(i=0; i < unList.length; i++){
   if (adduser == unList[i])
    window.alert = ""
 }
}
